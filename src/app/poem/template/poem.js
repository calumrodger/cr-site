import classes from './poem.module.scss';

const Poem = () => {

  return (
    <div className={classes.pageContainer}>
    <div className={classes.pageContent}>poem goes here</div>
    </div>
  );
};

export default Poem;
